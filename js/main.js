/* ============================================================
   Мой дневник — клиентские сценарии
   Лаб 4: массив записей + функции вывода/сортировки/фильтрации.
   Лаб 5: динамический DOM, форма добавления, валидация, localStorage.
   Лаб 6: загрузка записей через fetch() из data/posts.json.
   Лаб 13: оптимизация (делегирование, debounce, defer).
   ============================================================ */

// Уже пригодится в лабе 4 — текущий год в подвале сайта.
const yearEl = document.getElementById('year');
if (yearEl) {
  yearEl.textContent = new Date().getFullYear();
}