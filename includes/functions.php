<?php
// ============================================================
// includes/functions.php — вспомогательные функции (с лабы 7).
//   - получения записей (из массива → позже из БД);
//   - экранирования вывода (лаба 14):  e($str) = htmlspecialchars();
//   - валидации форм (лаба 8, 14).
// ============================================================

// Пример экранирования для защиты от XSS (уточняется в лабе 14).
function e(string $value): string {
    return htmlspecialchars($value, ENT_QUOTES, 'UTF-8');
}